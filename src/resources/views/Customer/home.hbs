{{> button}}
{{> foodCard}}
{{> tableCard }}
{{> loginForm }}
{{!-- {{> foodCard}} --}}
<section>
    {{#each data as |item|}}
        <div>
            <h1>{{item.name}}</h1>
            <small>{{item.price}}</small>
            <kbd>{{item.type}}</kbd>
            <ul>
                {{#each this.ingredients as |ingredient|}}
                    <li>{{ingredient}}</li>
                {{/each}}
            </ul>
        </div>
    {{/each}}
</section>
