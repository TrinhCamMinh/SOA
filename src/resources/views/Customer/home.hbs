{{!-- {{> foodCard}}
{{> modal}}
{{> button}}
{{> foodCard}}
{{> tableCard }}
{{> loginForm }} --}}
{{!-- {{> foodCard}} --}}
{{!-- <section>
    {{#each data as |item|}}
        <div>
            <h1>{{item.name}}</h1>
            <small>{{item.price}}</small>
            <kbd>{{item.type}}</kbd>
            <ul>
                {{#each this.ingredients as |ingredient|}}
                    <li>{{ingredient}}</li>
                {{/each}}
            </ul>
        </div>
    {{/each}}
</section> --}}

<div class='w-[300px] h-[500px] border-collapse bg-red-500'>
    <div class='w-full h-[50px] bg-orange-400'>
        <p>Ordering</p>
    </div>
    <div class=""> {{> orderingFood}} </div>
</div>